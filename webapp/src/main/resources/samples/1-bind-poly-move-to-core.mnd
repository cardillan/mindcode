// Declare a constant, so we can easily replace where we're going without
// having to rewrite everything, or recompile

TARGET = foundation1

addr_FLAG = 0

if cell1[addr_FLAG] == 0
    cell1[addr_FLAG] = rand(10**10)
end

while @unit == null
    print("Searching available unit...")
	printflush(message1)
    ubind(@poly)
end

if @unit.flag == 0
    flag(cell1[addr_FLAG])
end

move(TARGET.x, TARGET.y)
