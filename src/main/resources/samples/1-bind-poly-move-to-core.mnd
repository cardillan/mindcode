CORE = foundation1

if cell1[0] == 0 {
    cell1[0] = rand(10**10)
}

FLAG = cell1[0]

while @unit == null && @unit.flag == 0 {
    @unit = ubind(@poly)
    if @unit.flag == 0 {
        flag(FLAG)
    }
}

move(CORE.x, CORE.y)
