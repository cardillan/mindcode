CORE = foundation1
if cell1[0] == 0 {
    cell1[0] = rand(9**9**9)
}

while @unit == null {
    @unit = ubind(@poly)
    if @unit.flag != cell1[0] {
        @unit = null
    }
}

move(CORE.x, CORE.y)
