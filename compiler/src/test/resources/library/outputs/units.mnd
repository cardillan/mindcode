require units;

begin

    var initialFlag = null;
    var message = null;
    var preface = null;
    var radius = null;
    var unitType = null;
    var x = null;
    var y = null;

    println(noControlWithin(x, y, radius));
    println(findFreeUnit(unitType, initialFlag));
    println(findClosestUnit(x, y, unitType, initialFlag));
    println(waitForFreeUnit(unitType, initialFlag));
    println(waitForFreeUnit(message, preface, unitType, initialFlag));

end;