package info.teksol.mc.profile.options;

import org.jspecify.annotations.NullMarked;

@NullMarked
public enum CompilerOptions {
    ARRAY_ITERATION_THRESHOLD,
    ATOMIC_FULL_PROTECTION,
    ATOMIC_MERGE_LEVEL,
    ATOMIC_SAFETY_MARGIN,
    AUTO_PRINTFLUSH,
    EMULATE_STRICT_NOT_EQUAL,
    ENFORCE_SIZE_LIMITS,
    IPT,
    REMARKS,
    SETRATE,
    STACK_DEPTH,
    SYNTAX,
    TARGET_GUARD,
}
