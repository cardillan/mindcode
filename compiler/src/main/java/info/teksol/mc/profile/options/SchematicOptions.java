package info.teksol.mc.profile.options;

import org.jspecify.annotations.NullMarked;

@NullMarked
public enum SchematicOptions {
    ADD_TAG,
    ALLOW_LINK_GAPS,
    ALLOW_UNSATISFIED_LINKS,
    SCHEMATIC_TARGET,
}
